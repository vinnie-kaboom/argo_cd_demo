apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Default to prod overlay (ArgoCD applications will override via path)
resources:
  - overlays/prod
  - application.yaml         # prod Application
  - application-dev.yaml     # dev Application
  - application-staging.yaml # staging Application
